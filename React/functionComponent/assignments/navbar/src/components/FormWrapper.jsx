import Form from './Form'
import React from 'react'

const FormWrapper = (props) => {
    return (
        <fieldset>
            <legend>FormWrapper</legend>
            <Form />
        </fieldset>
    )
}

export default FormWrapper